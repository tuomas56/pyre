#(c) Tuomas Laakkonen 2015, under the MIT license.
#A sample Pyre library. It exports a simple API, which
#then is turned into a module by the import system.

module (triple, double) do
	let triple = def (n)
		n.mul(3)

	let double = def (n)
		n.mul(2)
end